// timeline.js - БАЗОВАЯ инициализация

export function initTimeline() {
    console.log('✅ Таймлайн загружен (только календарь и блоки)');
    
    // Просто проверяем наличие элементов
    const section = document.getElementById('timeline');
    if (section) {
        console.log('📌 Секция таймлайна найдена');
        console.log('📅 Календарь и 4 блока расписания готовы');
    }
    
    // Никакой сложной логики - только подтверждение
    return function cleanup() {
        // Ничего не нужно очищать
    };
}